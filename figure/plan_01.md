关于图的初步想法：
1、训练集和验证集结构域蛋白的酸碱性分布（分层选取数据，平衡样本数据量导致的训练偏差），序列长度分布，含配体的分布。
2、MoMPNN\LiganfMPNN\ConfuMPNN三者对照（天然native无条件直接生成能力，带配体的生成能力（对标LigandMPNN）、生成序列的Tm/Sol是否稳定（对标MoMPNN））。
3、电荷命中准确性、recovery、回折能力。
4、global校准表什么样，校准前后对比。
5、最终验证1：核糖体蛋白，先计算每个蛋白原始pI，然后在原pI下采样10条，然后再+-1下采样10条（环境微调），然后在+-3上采样10条（纯验证泛化能力）。
6、最终验证2：实验或采样比较之前用的分布设计和ConfuMPNN设计的准确性（这个数据没有上传）。实际实验表达情况的验证。
7、消融：各种控制条件模块的消融（不用非常细，取训练样本的10%，轮次变为原本的1/3，或者有哪几个版本是可以用的（比如课程学习开关的影响对比、直接比较替换课程学习的新模块的引入相比于原本的课程学习好在哪））、采样时开关的影响。其中各种模块加或每加可以按之前版本训练的模型进行采样测定。
8、取样ESMfold对比AF3
9、如果创新不够，可以看看数据量上堆一堆工作量。（但是目前应该够吧）