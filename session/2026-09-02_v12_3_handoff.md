# v12.3 交接文档（2026-09-02 训练中 → 续跑验证链）

> 本文件供 2026-09-02 训练完成后（预计 9/3 04:40）上下文重启/压缩后无缝续接。
> 若 resume 失败，主 session 可凭本文档直接接管。
> 执行记录见 `session/2026-09-02_v12_3_long_retrain.md`。

## 0. 现状一句话
v12.3（MoMPNN 蛋白模式扩长蛋白重训）正在 GPU6 跑 40 epoch（PID 1526600），
数据 6580 域（v12.2 可解析 6125 + S40 长域 455，L>400 占比 8.8%），超参 = v12.2 单一变量。
epoch 1/40 已完成（无 NaN，skip=0）。

## 1. 训练完成判据（唤醒后先检查）
```bash
# 40/40 完成
grep "epoch 40/40" log/v12_3_train_mompnn.log
# 全程无 NaN（应为 0）
grep -ciE "nan|traceback|out of memory" log/v12_3_train_mompnn.log
# checkpoint 完整
ls -la output/finetune_v12_3/finetune_epoch040.pt output/finetune_v12_3/condition_encoder_last.pt
# 进程应已退出（40/40 后）或 GPU6 显存回落
nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader | grep "^6,"
```
**epoch30 vs epoch40 收敛对比**（判断 40ep 是否够）：
```bash
grep -E "epoch (30|40)/40" log/v12_3_train_mompnn.log
# 对照 v12.2 30ep 末态 total≈4.198（epoch30/30 4.1985，已平台）。
# 若 v12.3 epoch30→40 total 降幅 <0.02 → plateau，可正常进入验证；若仍显著降 → 需续训（--epochs 更大 + 恢复 ckpt）。
```
异常（NaN/崩溃/ckpt 缺）→ 停下汇报，不进验证。

## 2. 验证链（v12.3 核心：长蛋白是否改进）

> 以下所有命令 cwd=项目根，python=`/home/baokun_yu/miniconda3/envs/confumpnn/bin/python`。
> 关键变量：ckpt=`output/finetune_v12_3/finetune_epoch040.pt`（或 condition_encoder_last.pt），
> backbone=`MoMPNN/mompnn_paper_checkpoints/mompnn_temberture_tm_esm_6_4_4_b01.ckpt`，
> manifest=`data/validation_pdbs/validation_manifest_v12_3.json`（9 单体：1AZM/1AS2/2FEO/5CQH/1CGE/1A65/1BJ4/13BB/1CDG），
> trainish list=`log/v12_3_protein_trainish.list`（7 域，已生成）。
> GPU：训练完 GPU6 空出；GPU4 是主 session v14（勿碰）；GPU0/1/2/5/7 他人满载；首选 cuda:6，竞争大则 cuda:3。

### ① 17(16) 蛋白响应诊断（slope 判据）
```bash
PYTHONPATH=code python index/v10_repair/v10_diag_response_curve.py \
  --cond_encoder output/finetune_v12_3/finetune_epoch040.pt \
  --weights MoMPNN/mompnn_paper_checkpoints/mompnn_temberture_tm_esm_6_4_4_b01.ckpt \
  --backbone auto --pdb-list log/v12_3_protein_trainish.list \
  --manifest data/validation_pdbs/validation_manifest_v12_3.json \
  --targets=-34,-30,-25,-20,-15,-10,-5,0,5,10,18 --include_native --n 20 \
  --device cuda:6 --out output/v12_3_diag_response.json
```
产物：`output/v12_3_diag_response.json`（16 蛋白 = 7 trainish + 9 valid）。
判据：valid 组（9 蛋白）线性区 slope ∈ [0.9,1.15]（trainish 作对照；若 valid≈trainish≈1 说明无过拟合）。
分析：`python index/v10_repair/analyze_diag.py --diag output/v12_3_diag_response.json`（若参数不符直接看 json）。

### ② 重拟合校准表（⚠️ 必须重拟合，v12.2 表不适用——分布变长）
```bash
PYTHONPATH=code python index/v10_repair/build_calibration.py \
  --diag output/v12_3_diag_response.json --label v12_3 \
  --out output/charge_calibration_v12_3.json
```
产物：`output/charge_calibration_v12_3.json`（global slope/intercept + per_protein）。
参照：`output/charge_calibration_v12_2.json`。

### ③ 泛化验证（H1 折叠 + H2 电荷 + GRAVY + H4）
主驱动 = `code/tests/ligand_v9/run_v12_1_validation.sh`（已封装 V1 采样 → V2 ESMFold → V3 TM → V4 统计 → V5 PROPKA）。**复制一份改成 v12.3 变量**：
```bash
cp code/tests/ligand_v9/run_v12_1_validation.sh /tmp/run_v12_3_validation.sh
# 改动：
#   MANIFEST = data/validation_pdbs/validation_manifest_v12_3.json
#   ENC      = output/finetune_v12_3/finetune_epoch040.pt
#   CAL      = output/charge_calibration_v12_3.json
#   OUT      = output/generalization_v12_3_calib
#   TARGET   = 45  (9 蛋白 × 5 臂) 及 count 判据 <9 validation.json
#   V2 ESMFold device: cuda:6（不要 GPU4，v14 在用）
#   V5 PROPKA PROTS = ['1BJ4','1A65','13BB','1CDG']（长蛋白；原脚本含已删 1AG0/1C6O 必须改）
#   propka outdir = output/propka_v12_3
nohup bash /tmp/run_v12_3_validation.sh > log/v12_3_validation.stdout 2>&1 &
```
产物：
- `output/generalization_v12_3_calib/protein/{pdb}/validation.json`（每蛋白，含 arm 电荷/回收/GRAVY）
- `output/generalization_v12_3_calib/protein/{pdb}/pH7.4/arm_{arm}/folds/`（ESMFold 回折）
- `.../arm_{arm}/tm.csv`（US-align TM vs `OUT/ref/{pdb}_ref.pdb`）
- `output/generalization_v12_3_calib_stats.json`（V4 汇总）
- `output/propka_v12_3/{pdb}_{arm}.json`（V5 H4）

### ④ H3 电荷聚集合法性（可选但建议）
```bash
PYTHONPATH=code python code/tests/h3_charge_legality.py \
  --gen-root output/generalization_v12_3_calib --ref-root <ref根(见下)> \
  --native-root <native_ref fasta> --uncond-root <无条件基线 fasta> \
  --pH 7.4 --out output/h3_protein_v12_3.json
```
ref 根：`{OUT}/ref/`（write_ref_skeleton 产物，run_v12_1_validation.sh V3 已生成）；native_ref 由
`code/tests/v12_2_extract_native.py` 生成；无条件基线由 `code/tests/ligand_v9/sample_unconditioned.py`
（`--manifest ... --cond_encoder ckpt --weights W --n 30 --device cuda:6`）生成。
判据：条件臂违规率 ≤ max(native, 无条件) + 5pp。v12.2 蛋白基线见 `output/h3_protein.json`。

### ⑤ Tm/Sol 物化验证（可选，长蛋白重点看）
流程照 `analysis/report/2026-08-31_v12_2_tm_sol.md`：无条件基线采样 → Tm(TemBERTure `temberture_score.py`, HF_HUB_OFFLINE=1) →
Sol(`protein_sol_predict.py`) → 汇总 `code/tests/v12_2_tm_sol_summarize.py`（v12.3 换 OUT 目录）。
**无条件基线占位 = net_charge 训练均值（勿用 None，会 poly-G 塌缩）**。

## 3. 与 v12.2 对照的核心判据
| 判据 | v12.2 蛋白（基准） | v12.3 目标 |
|---|---|---|
| valid slope（校准前） | 1.00（已达标） | ∈[0.9,1.15] |
| 泛化 H2（per-protein 校准，9 蛋白） | 72%（10 蛋白） | ≥72% |
| **长蛋白 H2：1A65(504)/1BJ4(470)/13BB(560)/1CDG(686)** | 1A65/1BJ4 曾 fail（长 OOD） | **改进 = 本轮核心结论** |
| H1 折叠 TM≥0.7（单体） | 8 健康蛋白全过 | 9 单体全过（无二聚体） |
| H4 PROPKA | 6/8 | 长蛋白 PASS |
| Tm/Sol S2 | 0/50 无恶化 | 无恶化（长蛋白别恶化） |
| 组成 D/K vs native | 健康 | 长蛋白组成健康（防删减/过度添加） |

## 4. 已知坑
1. **校准表必须重拟合**（v12.3 分布变长，v12.2 表 slope/intercept 不适用）。
2. **GPU 分配**：GPU4 = 主 session v14 配体训练（勿碰）；GPU0/1/2/5/7 他人满载；GPU6 训练完空出但可能有他人小进程占 util（使 epoch 变慢 ~2×，不影响正确性）。ESMFold 环境 python = `/home/baokun_yu/miniconda3/envs/confumpnn-esmfold/bin/python`。
3. **manifest v12_3 已删二聚体**（1C6O/1AXW/1AG0）→ 泛化 H1 只评单体；诊断/校准也以 9 valid 蛋白为准（trainish 7 域保持）。
4. **run_v12_1_validation.sh 硬编码要改**：V2 用 GPU4、V5 PROTS 含已删 1AG0/1C6O、TARGET 计数按 10 蛋白——复制后必须改。
5. **长蛋白 1A65 响应弯曲**（校准敏感，LOOCV 大）→ 若 per-protein 不稳自动回退 global，勿手动加大 n_per（n20 会退化）。
6. **labels 口径**：v12.3 训练 = 6580 域（v12.2 "6710" 里 585 ext 从未被训、实际只训 6125）；对比 v12.2 验证结果时理解此差异。
7. **pH/charge 段错位 bug（train_finetune）**：任何坏域 skip 都会使其后所有域标签错位——v12.3 labels 已全量预检 6580/6580 可解析（skip=0），**勿再往 labels 里加未预检域**。
8. 保存纪律：每完成 ①-⑤ 一步即更新本 handoff + `session/2026-09-02_v12_3_long_retrain.md` → git commit（log/output/data 不提交）。

## 5. 产物清单（v12.3 预期）
- 训练：`output/finetune_v12_3/`（epoch001-040 + condition_encoder_last.pt）；日志 `log/v12_3_train_mompnn.{log,stdout}`
- 诊断：`output/v12_3_diag_response.json`；校准表：`output/charge_calibration_v12_3.json`
- 泛化：`output/generalization_v12_3_calib/` + `output/generalization_v12_3_calib_stats.json`
- H4：`output/propka_v12_3/`；H3：`output/h3_protein_v12_3.json`；Tm/Sol：`output/tm_sol_v12_3/`
- 报告：`analysis/report/2026-09-02_v12_3_{diag,validation,summary}.md`
